{
  "name": "lambda-error-reporting",
  "version": "1.0.0",
  "description": "Reports to Slack Webhook if a lambda function has failed withing a specified time range",
  "main": "dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "test": "jest --collectCoverage  --errorOnDeprecated --logHeapUsage --forceExit --runInBand test/**",
    "build": "npx tsc",
    "compile": "rm -rf dist && npm run build",
    "prepack": "npm run compile",
    "tslint": "tslint 'src/**/*.ts' 'test/**/*.ts'",
    "tslint:fix": "tslint --fix 'src/**/*.ts' 'test/**/*.ts'"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.68",
    "@types/jest": "^26.0.14",
    "@types/lodash": "^4.14.162",
    "@types/node": "^14.10.1",
    "aws-sdk": "^2.752.0",
    "jest": "^26.4.2",
    "jest-cli": "^26.4.4",
    "nock": "^13.0.4",
    "ts-jest": "^26.3.0",
    "tslint": "^6.1.3",
    "typemoq": "^2.1.0",
    "typescript": "^4.1.3"
  },
  "engines": {
    "node": ">= 12 < 13",
    "npm": ">= 6.9.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ringier-data/rcplus-monitoring.git",
    "directory": "src/lambda-error-reporting"
  },
  "keywords": [
    "Ringier",
    "Error",
    "Reporting",
    "Monitoring"
  ],
  "author": "Connect+ Development Team",
  "license": "SEE LICENSE IN LICENSE"
}
